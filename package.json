{
  "name": "redaxa",
  "version": "0.3.1",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "build:desktop": "npm run build && node scripts/build-desktop.mjs",
    "build:web": "npm run build && node scripts/build-web.mjs",
    "check": "tsc --noEmit",
    "test": "tsx scanner.test.ts && tsx policy.test.ts && tsx tokens.test.ts && tsx api/_billing.test.ts && tsx api/auth/session.test.ts && tsx api/_email.test.ts && tsx api/_audit.test.ts && tsx api/stripe-webhook.test.ts",
    "start": "npm run build && tsx server.ts",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-clipboard-manager": "^2.3.2",
    "@tauri-apps/plugin-global-shortcut": "^2.3.2",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "stripe": "^22.5.0"
  }
}
